<resources>

    <!--  application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:actionBarStyle">@style/actionBarStyle1</item>
        <!-- support library -->
        <item name="actionBarStyle">@style/actionBarStyle1</item>
    </style>

    <style name="actionBarStyle1" parent="@style/Widget.AppCompat.Light.ActionBar.Solid.Inverse">
        <item name="android:background">@color/chronology_color</item>
        <!-- support library -->
        <item name="background">@color/chronology_color</item>
    </style>

</resources>
